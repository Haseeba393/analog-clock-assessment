import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: {
        position: 'absolute',
        transformOrigin: 'bottom',
        bottom: '50%',
    },
    landscapeContainer: {
        position: 'absolute',
        transformOrigin: 'bottom',
        bottom: '25%',
    },
});